name: Devops Pipeline

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - users: actions/checkout@v4
      
      - users: actions/setup-python@v5
        with:
         python-version:'3.12'

        -run: python app.py
      
